{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: core APIs only. Every future capability (filesystem, shell, screen) must be added explicitly and reviewed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
